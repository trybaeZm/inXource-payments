import axios from 'axios'

class Auth{
    async signUp(){
        const res = await axios.post();
        return res;
    }

    async login(){
        const res = await axios.post();
        return res;
    }

    async getDetails(){
        const res = await axios.post();
        return res;
    }
}

export default new Auth;